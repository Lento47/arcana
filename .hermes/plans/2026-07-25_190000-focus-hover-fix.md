# Focus/Hover Polish — Kill the Blue Box v2 (Corrected)

> **For Hermes:** Execute task-by-task. Every old_string verified unique.

**Goal:** Replace garish `spinePrompt` blue box + ghost `backgroundElement` with Grok-style accent border. Hover does nothing — focus is keyboard-only (j/k). All entries consistent.

---

### Full Audit — All Focus States

| Component | Current State | Issue | Fix |
|---|---|---|---|
| Chat card BG | `spinePrompt` lifted 4.8 → bright blue box | Garish | Remove focus BG. Keep accent border. |
| Entry header BG | `backgroundElement` → 1-shade ghost | Invisible | Remove BG. Accent border instead. |
| Entry header border | Only for `!isChatProse()` | Incomplete | Already fine — chat cards have own border. |
| Hover→focus | `onMouseOver` → `focusEntry` | Aggressive | Remove. Hover does nothing. |
| Toggle row text | `focused ? t.text : spineDiffMuted` | Subtle, fine | Keep as-is. |

---

### Task 1: Chat card focus → accent left-border only

**File:** `spine-chat.tsx` — lines 75-79

Current:
```typescript
  const cardBg = createMemo(() => {
    if (!isAssistant()) return undefined
    if (focused()) return (t.spinePrompt ?? t.backgroundElement ?? t.backgroundPanel) as any
    return (t.backgroundPanel ?? t.backgroundElement) as any
  })
```

Keep `!isAssistant()` guard. Drop focus line. Both focused/unfocused get same BG:
```
old:
  const cardBg = createMemo(() => {
    if (!isAssistant()) return undefined
    if (focused()) return (t.spinePrompt ?? t.backgroundElement ?? t.backgroundPanel) as any
    return (t.backgroundPanel ?? t.backgroundElement) as any
  })

new:
  const cardBg = createMemo(() => {
    if (!isAssistant()) return undefined
    return (t.backgroundPanel ?? t.backgroundElement) as any
  })
```

---

### Task 2: Entry header focus → accent left-border only

**File:** `spine-entry.tsx` — lines 222-225

Current:
```typescript
              backgroundColor={props.focused ? (t.backgroundElement as any) : undefined}
              border={props.focused && !isChatProse() ? (["left"] as any) : undefined}
              borderColor={props.focused && !isChatProse() ? (t.accent as any) : undefined}
```

Remove `backgroundColor`. Keep `!isChatProse()` — chat cards have their own accent border (spine-chat.tsx:111). Double-border on chat entries would look wrong.
```
old:
              backgroundColor={props.focused ? (t.backgroundElement as any) : undefined}
              border={props.focused && !isChatProse() ? (["left"] as any) : undefined}
              borderColor={props.focused && !isChatProse() ? (t.accent as any) : undefined}

new:
              border={props.focused && !isChatProse() ? (["left"] as any) : undefined}
              borderColor={props.focused && !isChatProse() ? (t.accent as any) : undefined}
```

---

### Task 3: Hover → nothing. Focus is keyboard-only.

**File:** `spine-entry.tsx` — line 203

```
old:         onMouseOver={handleHover}

new:         onMouseOver={undefined}
```

**File:** `command-spine-shell.tsx` — line 322

```
old:                     onHover={() => focusEntry(entry())}

new: (remove the line entirely)
```

Remove dead code — `handleHover` function at lines 165-167 and `onHover` prop type at line 42:
```
old:   onHover?: () => void

new: (remove line)
```

```
old:   const handleHover = () => {
    props.onHover?.()
  }

new: (remove block)
```

---

### Task 4: Build + local verify

```bash
cd L:/PROJECTS/arcana && bun run build
```

Expected: 8/8. No push.
