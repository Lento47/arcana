# Subagent Polish — Final Execution Plan

**Goal:** Polish subagent experience to exceptional quality. Zero regressions, zero new bugs.

**Audit findings from subagent 1 (interrupted but collected data):**
- `subagent-footer.tsx`: functional, handles all states (pending/running/done), shows usage+context+cost
- `move.tsx`: handles session move between workspaces
- No major gaps found — footer is solid

**My direct audit findings:**
1. Open button: no visual affordance (just text, no rail alignment)
2. No distinction between "running" vs "completed" child
3. No separator between agent body and open button
4. Missing hover state on clickable text

---

## Tasks (execute sequentially)

### Task 1: Polish Open session button with rail alignment + hover state

**File:** `spine-entry.tsx:328-335`

Current bare button → polished button with spine rail alignment, topic separator line, hover highlight.

### Task 2: Show running vs completed state in Open button

When `source.sessionID` exists but child is still running → show "⤷ subagent running…"
When child is done → show "⤷ Open session"

### Task 3: Build + typecheck + verify zero regressions
