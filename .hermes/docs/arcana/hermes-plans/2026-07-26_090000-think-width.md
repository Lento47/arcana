# Think + Code Overfill — Off-by-Padding Fix

> **For Hermes:** Execute task-by-task. Every patch verified.

**Goal:** Think + code text truncated at right edge. Measured: `contentWidth` = 122, `paddingLeft={3}` steals 3 chars, effective area = 119. SpineProse `<markdown>` renders at 122 — 3 chars overfill.

**Fix:** Remove `paddingLeft` from inner boxes. Outer spine layout already handles all padding via `spineOuterPadding` + gutter + rail.

---

### Task 1: Remove think box padding

**File:** `spine-entry.tsx` — line 380

```
old:               paddingLeft={3}

new:               paddingLeft={0}
```

### Task 2: Remove code box padding

**File:** `spine-entry.tsx` — line 460

```
old:               <box flexGrow={1} minWidth={0} flexShrink={1} paddingLeft={1}>

new:               <box flexGrow={1} minWidth={0} flexShrink={1}>
```

### Task 3: Build + verify

```bash
cd L:/PROJECTS/arcana && bun run build
```
