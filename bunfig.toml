# Solid transform for TUI when launching from monorepo root.
# Package-level bunfig is not applied when bun cwd is the repo root.
preload = ["./scripts/tui-solid-preload.ts"]

[install]
exact = true

[install.lockfile]
print = "bun"

[test]
preload = ["./scripts/tui-test-preload.ts"]
