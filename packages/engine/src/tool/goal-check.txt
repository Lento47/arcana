CHECK IN on the active session goal. Call periodically during multi-step work.

status values:
- in_progress: still working
- complete: goal achieved (freezes mutation tools until a new goal_set)
- blocked: cannot proceed without user help
- stale: goal may no longer match user intent

When complete, stop mutating and summarize for the user. Set a new goal only if the user starts a new objective.