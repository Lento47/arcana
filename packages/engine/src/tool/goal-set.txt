RECORD the user's explicit multi-step mutation objective for this session.

The goal is binding — multi-step mutation tools (edit/write/bash) for build/general/tester require an active goal.
Do not call this for greetings, banter, explanations, reviews, or simple read-only requests. User can also set goals with /goal in the TUI.

Do not invent a different goal mid-run; update only if the user clearly changes objectives.
