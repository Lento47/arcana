RECORD the user's stated goal for this session. Call once you understand what they want done.

The goal is binding — multi-step mutation tools (edit/write/bash) for build/general/tester require an active goal.
Prefer calling this early. User can also set goals with /goal in the TUI.

Do not invent a different goal mid-run; update only if the user clearly changes objectives.