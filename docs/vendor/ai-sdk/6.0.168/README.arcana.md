# Vendored AI SDK documentation

- **Package:** `ai`
- **Version:** `6.0.168`
- **Declared source:** `https://registry.npmjs.org/ai/-/ai-6.0.168.tgz`
- **Vendored content:** the package's bundled `docs/` directory only
- **Purpose:** version-matched engineering reference for Arcana's AI SDK integration
- **Inventory:** 235 `.mdx` files; no symlinks; commit all documentation with regular non-executable mode `100644`
- **Lockfile package integrity:** `sha512-2HqCJuO+1V2aV7vfYs5LFEUfxbkGX+5oa54q/gCCTL7KLTdbxcCu5D7TdLA5kwsrs3Szgjah9q6D9tpjHM3hUQ==`
- **Documentation manifest SHA-256:** `95324a4b5078c6857d330922a749b4ac87e73e2076d12b21addce4c9cf8d5824`

These files are upstream reference material. Keep the version directory immutable; when Arcana changes its pinned AI SDK version, add a new version directory instead of silently replacing this one.

No package was installed to create this reference folder.

The manifest digest is the SHA-256 of the sorted `shasum -a 256` lines for the 235 `.mdx` files, run from the repository root. It detects later changes to this checked-in snapshot; it is not an independent upstream signature. The package-level integrity value is copied from `bun.lock`.

During the 2026-07-14 review, `diff -qr` found no difference between these 235 files and `packages/engine/node_modules/ai/docs` from the already-present local `ai@6.0.168` package. No dependency installation or network fetch was performed for that check.
