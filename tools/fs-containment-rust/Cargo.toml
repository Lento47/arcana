[package]
name = "fs-containment"
version = "0.1.0"
edition = "2021"
description = "D-7.1: Linux kernel-enforced filesystem containment via openat2"

[dependencies]
libc = "0.2"

[dev-dependencies]
tempfile = "3"
